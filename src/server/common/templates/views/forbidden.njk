<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forbidden - {{ serviceName }}</title>
    <link href="https://www.gov.uk/static/govuk-frontend-5.11.0.min.css" rel="stylesheet">
</head>
<body>
    <div class="govuk-width-container">
        <main class="govuk-main-wrapper" id="main-content" role="main">
            <div class="govuk-grid-row">
                <div class="govuk-grid-column-two-thirds">
                    <h1 class="govuk-heading-xl">
                        You are not allowed to access this page
                    </h1>

                    {% if errorMessage %}
                    <div class="govuk-inset-text">
                        <p class="govuk-body">
                            <strong>Error details:</strong> {{ errorMessage }}
                        </p>
                    </div>
                    {% endif %}

                    <p class="govuk-body">
                        Your session may have expired or you may not have the necessary permissions to view this content.
                    </p>

                    <p class="govuk-body">
                        Please check your credentials and try again.
                    </p>

                    <div class="govuk-button-group">
                        <a href="javascript:history.back()" class="govuk-button govuk-button--secondary">
                            Go back
                        </a>
                    </div>
                </div>
            </div>
        </main>
    </div>
</body>
</html>
